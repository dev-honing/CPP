#include <iostream>
#include <string>

int main(void) {

    // 변수의 선언과 할당, 초기화
    int a; // 변수 선언
    a = 10; // 할당(대입 연산자를 사용해 10이라는 값을 저장)
    

    int a = 10; // 변수 선언과 동시에 초기화

    return 0;
}
